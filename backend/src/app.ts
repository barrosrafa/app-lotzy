import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import { randomUUID } from 'node:crypto'; import rateLimit from 'express-rate-limit'; import { env } from './shared/config/env.js'; import { errorHandler } from './infrastructure/http/middlewares/errorHandler.js'; import { gameRoutes, toolRoutes } from './infrastructure/http/routes/api.routes.js'; import { advancedRoutes } from './infrastructure/http/routes/advanced.routes.js'; import { openapi } from './infrastructure/http/openapi/openapi.js';
export function createApp(){const app=express();app.disable('x-powered-by');app.set('trust proxy',env.NODE_ENV==='production'?1:false);app.use((req,res,next)=>{const id=String(req.headers['x-request-id']??randomUUID());req.headers['x-request-id']=id;res.setHeader('X-Request-Id',id);next();});app.use(helmet());app.use(cors({origin:env.CORS_ORIGINS.length?env.CORS_ORIGINS:true,methods:['GET','POST','OPTIONS']}));app.use(express.json({limit:'256kb'}));app.get('/health',(_req,res)=>res.json({status:'ok'}));app.get('/ready',(_req,res)=>res.json({status:'ready',checks:{priceTable:true,wheelCatalog:true}}));app.get('/metrics',(_req,res)=>res.type('text/plain').send('# HELP lotzy_up API liveness\n# TYPE lotzy_up gauge\nlotzy_up 1\n'));app.get('/api/v1/openapi.json',(_req,res)=>res.json(openapi));const standard=rateLimit({windowMs:env.RATE_LIMIT_WINDOW_MS,max:env.RATE_LIMIT_MAX,standardHeaders:true,legacyHeaders:false});const heavy=rateLimit({windowMs:env.RATE_LIMIT_WINDOW_MS,max:10,standardHeaders:true,legacyHeaders:false});app.use('/api/v1/games/expand',heavy);app.use('/api/v1/tools/backtest',heavy);app.use('/api/v1/games',standard,gameRoutes);app.use('/api/v1/games',heavy,advancedRoutes);app.use('/api/v1/tools',standard,toolRoutes);app.use(errorHandler);return app;}
