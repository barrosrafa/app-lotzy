'use client';
import { useEffect, useState } from 'react';
import { generateCycle, getCycles, getDelays, getTemperature, type CycleResponse, type StatsDelay, type StatsTemperature } from '@/lib/api';
import { PageShell } from '@/app/aux-pages';
export default function StatisticsPage() { const [delays,setDelays]=useState<StatsDelay[]>([]); const [temperature,setTemperature]=useState<StatsTemperature>(); const [cycles,setCycles]=useState<CycleResponse>(); const [windowSize,setWindowSize]=useState<10|20|50>(20); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(true); useEffect(()=>{Promise.all([getDelays(),getTemperature(windowSize),getCycles()]).then(([d,t,c])=>{setDelays(d.data);setTemperature(t);setCycles(c);}).catch(e=>setMessage(e instanceof Error?e.message:'Não foi possível carregar as estatísticas.')).finally(()=>setLoading(false));},[windowSize]); async function cycleGame(){if(!cycles)return;setMessage('Gerando…');try{const result=await generateCycle(1,Math.max(15,cycles.dezenasFaltantes.length));sessionStorage.setItem('lotzy:validated-games',JSON.stringify({batch:{disclaimer:result.disclaimer},analysis:{data:result.data,aggregate:{meanSum:0,stdDevSum:0,meanPopularity:0}}}));setMessage('Jogo gerado com as dezenas faltantes. Abra Analisar para revisar.');}catch(e){setMessage(e instanceof Error?e.message:'Não foi possível gerar.');}} return <PageShell eyebrow="04 · Estatísticas" title="Leia o histórico sem transformá-lo em promessa." intro="Frequência e atraso ajudam a explorar os resultados anteriores; nenhuma métrica muda a probabilidade do próximo sorteio.">{loading?<div className="paper-card">Carregando histórico…</div>:<><div className="stat-grid"><div className="stat"><span>Ciclo atual</span><b>{cycles?.cicloAtual ?? '—'}</b></div><div className="stat"><span>Concursos no ciclo</span><b>{cycles?.concursosNoCiclo ?? '—'}</b></div><div className="stat"><span>Dezenas faltantes</span><b>{cycles?.dezenasFaltantes.join(', ') || 'Nenhuma'}</b></div></div><div className="controls"><label>Janela de temperatura<select value={windowSize} onChange={e=>setWindowSize(Number(e.target.value) as 10|20|50)}><option value={10}>10 concursos</option><option value={20}>20 concursos</option><option value={50}>50 concursos</option></select></label><button className="btn btn-primary" onClick={cycleGame} disabled={!cycles?.dezenasFaltantes.length}>Gerar jogo do ciclo</button></div><div className="page-grid"><section className="paper-card"><h2>Temperatura · {temperature?.janela}</h2><div className="result-list">{temperature?.data.map(item=><div className="game-row" key={item.dezena}><span className="game-index">{String(item.dezena).padStart(2,'0')}</span><div style={{flex:1}}><div style={{height:8,borderRadius:99,background:'var(--ink)',width:`${Math.max(3,item.percentual)}%`}} /></div><span className="status">{item.frequencia}x</span></div>)}</div></section><section className="paper-card"><h2>Maiores atrasos atuais</h2><div className="result-list">{[...delays].sort((a,b)=>b.atrasoAtual-a.atrasoAtual).slice(0,10).map(item=><div className="game-row" key={item.dezena}><span className="game-index">Dezena {item.dezena}</span><span className="status">{item.atrasoAtual} concursos</span></div>)}</div></section></div><div className="disclaimer"><p>{cycles?.disclaimer}</p></div>{message&&<div className="error" role="status">{message}</div>}</>}</PageShell>; }
