'use client';
import { useEffect, useState } from 'react';
import { PageShell } from '@/app/aux-pages';
export default function NotificationsPage(){const [enabled,setEnabled]=useState(false);const [time,setTime]=useState('18:00');useEffect(()=>{setEnabled(localStorage.getItem('lotzy:lembrete')==='1');setTime(localStorage.getItem('lotzy:lembrete-hora')??'18:00');},[]);function save(){localStorage.setItem('lotzy:lembrete',enabled?'1':'0');localStorage.setItem('lotzy:lembrete-hora',time);}return <PageShell eyebrow="08 · Lembretes" title="Lembretes ficam sob seu controle." intro="Nesta versão não há push nem envio de dados. Guardamos apenas a preferência local para uma futura integração explícita."><div className="paper-card"><label><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)} /> Ativar lembrete local</label><label style={{marginTop:18}}>Horário<input type="time" value={time} onChange={e=>setTime(e.target.value)} /></label><button className="btn btn-primary" style={{marginTop:20}} onClick={save}>Salvar neste dispositivo</button></div></PageShell>}
