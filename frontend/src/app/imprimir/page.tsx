'use client';
import { useEffect, useState } from 'react';
export default function PrintPage(){const [games,setGames]=useState<number[][]>([]);useEffect(()=>{const raw=sessionStorage.getItem('lotzy:validated-games');if(raw)try{setGames(JSON.parse(raw).analysis.data.map((entry:{game:number[]})=>entry.game));}catch{/* vazio */}},[]);return <main className="main print-page"><div className="controls no-print"><button className="btn btn-primary" onClick={()=>window.print()}>Imprimir / salvar PDF</button></div><h1>Jogos Lotzy</h1><p className="no-print">Imprima esta folha para revisão pessoal. A impressão não registra aposta.</p><div className="print-grid">{games.map((game,index)=><article className="paper-card" key={index}><h2>Jogo {index+1}</h2><div className="mini-grid">{game.map(n=><span className="mini-number" key={n}>{String(n).padStart(2,'0')}</span>)}</div></article>)}</div><style jsx>{`@media print { .no-print{display:none!important} .print-page{padding:20px} .print-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.paper-card{box-shadow:none;break-inside:avoid} }`}</style></main>}
